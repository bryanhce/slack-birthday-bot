{
  "name": "slack-birthday-bot",
  "version": "1.0.0",
  "description": "A Slack bot for managing birthday reminders",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && serverless deploy",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.859.0",
    "@aws-sdk/lib-dynamodb": "^3.859.0",
    "@slack/bolt": "^3.14.0",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1490.0",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.8.7",
    "serverless": "^3.36.0",
    "serverless-esbuild": "^1.55.1",
    "serverless-plugin-typescript": "^2.1.4",
    "typescript": "^5.2.2"
  }
}
